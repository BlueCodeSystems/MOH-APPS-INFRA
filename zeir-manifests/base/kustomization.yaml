resources:
  - storage.yaml
  - zeir-deployment.yaml
  - secrets.yaml
  - ingress.yaml
vars:
  - name: mysql-service
    objref:
      kind: Service
      name: mysql-service
      apiVersion: v1
    fieldref:
      fieldpath: metadata.name
  - name: postgres-service
    objref:
      kind: Service
      name: postgres-service
      apiVersion: v1
    fieldref:
      fieldpath: metadata.name
